# SHOWCASE: text robustness perturbation — the `augmenter` node (architecture §5, C2).
# An explicit graph inserts an augmenter between ASR and embedding: it corrupts each
# query deterministically (seeded per item) with ASR-confusion homophones and the
# clinically dangerous dose/unit swaps (mg <-> mcg) that drive the CEER metric. Compare
# this run's leaderboard entry against showcase_asr_model_compare's clean whisper_base
# branch to quantify retrieval robustness under realistic transcription noise.
experiment:
  name: showcase_robustness_augmenter
  output_dir: evaluation_results/showcase
dataset:
  id: pubmed_qa
  questions: examples/data/pubmed_qa_small/questions.json
  corpus: examples/data/pubmed_qa_small/corpus.json
  trace_limit: 5
  batch_size: 4
data:
  num_workers: 0
graph:
  nodes:
  - dataset_source
  - corpus_embedding
  - vector_db
  - id: tts
    type: tts
    params:
      provider: mms
      voice: en
      sample_rate: 16000
      seed: 42
      output_dir: evaluation_results/showcase/audio
  - asr
  - id: augmenter
    type: augmenter
    params:
      homophones: true
      unit_corruption: true
      max_edits: 2
  - text_embedding
  - retrieval
  - metrics
  - finalize
  edges:
  - {from: dataset_source, to: corpus_embedding, input: corpus}
  - {from: corpus_embedding, to: vector_db, input: corpus_vectors}
  - {from: dataset_source, to: tts, input: query_text}
  - {from: dataset_source, to: asr, input: query_audio}
  - {from: tts, to: asr, input: query_audio}
  - {from: dataset_source, to: asr, input: reference_transcription}
  - {from: dataset_source, to: augmenter, input: query_text}
  - {from: asr, to: augmenter, input: query_text}
  - {from: dataset_source, to: augmenter, input: corpus}
  - {from: augmenter, output: augmented_query_text, to: text_embedding, input: query_text}
  - {from: dataset_source, to: text_embedding, input: query_text}
  - {from: asr, to: text_embedding, input: query_text}
  - {from: text_embedding, output: text_query_vectors, to: retrieval, input: query_vectors}
  - {from: vector_db, to: retrieval, input: vector_index}
  - {from: augmenter, output: augmented_query_text, to: retrieval, input: query_text}
  - {from: dataset_source, to: retrieval, input: query_text}
  - {from: asr, to: retrieval, input: query_text}
  - {from: dataset_source, to: retrieval, input: reference_transcription}
  - {from: dataset_source, to: retrieval, input: relevant_docs}
  - {from: retrieval, to: metrics, input: retrieved}
  - {from: dataset_source, to: metrics, input: query_text}
  - {from: asr, to: metrics, input: query_text}
  - {from: dataset_source, to: metrics, input: reference_transcription}
  - {from: dataset_source, to: metrics, input: relevant_docs}
  - {from: metrics, to: finalize, input: metrics}
  - {from: retrieval, to: finalize, input: retrieved}
nodes:
  asr:
    model: whisper
    size: base
    device: cuda:0
  text_embedding:
    model: labse
    device: cuda:0
  vector_db:
    store: inmemory
  retrieval:
    k: 10
runtime:
  cache:
    enabled: true
  logging:
    console_level: WARNING
